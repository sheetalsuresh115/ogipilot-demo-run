<!DOCTYPE html>
<html>
  <head>
    <title>Risk Demonstration</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    <%# <%= javascript_include_tag 'application' %>
    <%# <%= javascript_include_tag 'riskdashboard' %>
    <%# <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/chart.js@2.9.1/dist/Chart.min.css"> %>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
  </head>

  <body>
    <div class="container">
      <div class="row">
        <div class="col">
          <header>
            <div class="navbar navbar-dark bg-dark box-shadow">
              <div class="container d-flex justify-content-between">
                <a href="/" class="navbar-brand d-flex align-items-center">
                  <strong >Risk Demonstration DashBoard</strong>
                </a>
                <div class="dropdown">
                  <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <span class="navbar-toggler-icon"></span>
                  </button>
                  <ul class="dropdown-menu">
                    <li><%= button_to "Session Management", ogi_pilot_sessions_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "Job Management", sidekiq_web_path, method: 'get', class: "btn" %></li>
                  </ul>
                
                  <button class="btn btn-secondary dropdown-toggle" type="button" data-bs-toggle="dropdown" aria-expanded="false">
                    <span>Events</span>
                  </button>
                  <ul class="dropdown-menu">
                    <li><%= button_to "Baseline Risk Info", baseline_risk_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "Failure Event", load_alarms_component_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "M1 Active Failed", load_alarms_component_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "M2 StandBy Active", load_alarms_component_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "M2 Risk Info Update", load_alarms_component_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "New Motor Installed", load_alarms_component_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "Updated Operation Risk Info", load_alarms_component_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "Updated Risk Info", load_alarms_component_path, method: 'get', class: "btn" %></li>
                    <li><%= button_to "Updated Last Risk Info", load_alarms_component_path, method: 'get', class: "btn" %></li>
                  </ul>
                </div>
              </div>
            </div>
          </header>
          <%= yield %>
        </div>
      </div>
    </div>
    <!-- Bootstrap core JavaScript
    <!-- Placed at the end of the document so the pages load faster -->
    <%# <script>window.jQuery || document.write('<script src="../../../../assets/js/vendor/jquery-slim.min.js"><\/script>')</script> %>
  </body>
</html>
   